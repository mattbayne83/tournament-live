name = "tournament-manager"
compatibility_date = "2026-07-01"
pages_build_output_dir = "dist"

# Create the namespace with: npx wrangler kv namespace create TOURNAMENTS
# then paste the real id below before the first deploy.
[[kv_namespaces]]
binding = "TOURNAMENTS"
id = "placeholder-create-me"
